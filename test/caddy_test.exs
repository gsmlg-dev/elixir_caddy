defmodule CaddyTest do
  use ExUnit.Case
  # doctest Caddy

  # setup_all do
  #   Caddy.start()

  #   on_exit(fn ->
  #     nil
  #     # Caddy.stop()
  #   end)
  # end

  # test "test caddyfile" do
  #   assert Caddy.Config.get(:caddy_bin) =~ "bin/caddy"
  # end
end
